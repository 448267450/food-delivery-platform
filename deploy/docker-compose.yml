version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  user-service:
    build:
      context: ../
      dockerfile: ./deploy/Dockerfile.user
    ports:
      - "8081:8081"
    environment:
      DATABASE_HOST: postgres
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_DBNAME: food_delivery_users
      JWT_SECRET: super-secret-change-in-prod
    depends_on:
      postgres:
        condition: service_healthy

  # restaurant-service:
  #   build:
  #     context: ../
  #     dockerfile: ./deploy/Dockerfile.restaurant
  #   ports:
  #     - "8082:8082"
  #   environment:
  #     DATABASE_HOST: postgres
  #     DATABASE_USER: postgres
  #     DATABASE_PASSWORD: postgres
  #     DATABASE_DBNAME: food_delivery_restaurants
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

  # order-service:
  #   build:
  #     context: ../
  #     dockerfile: ./deploy/Dockerfile.order
  #   ports:
  #     - "8083:8083"
  #   environment:
  #     DATABASE_HOST: postgres
  #     DATABASE_USER: postgres
  #     DATABASE_PASSWORD: postgres
  #     DATABASE_DBNAME: food_delivery_orders
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  postgres_data:
